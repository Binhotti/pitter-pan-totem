<?php

/**
 * Arquivo: app.php
 * Camada: Config
 *
 * Responsabilidade:
 *   Configurações gerais da aplicação: nome do sistema, URL base, ambiente (desenvolvimento/produção), timezone, idioma padrão.
 *
 * Permitido conter:
 *   Apenas pares chave-valor de configuração (array PHP).
 *
 * Proibido conter:
 *   Lógica, condicionais complexas, acesso a banco.
 *
 * Depende de:
 *   Nenhuma (arquivo-folha).
 *
 * Usado por:
 *   app/Bootstrap/app.php e qualquer Service que precise de configuração geral.
 *
 * NOTA IMPORTANTE:
 *   Este arquivo é um esqueleto arquitetural (parte da fundação do
 *   sistema Pitter Pan). Nenhuma funcionalidade, lógica de negócio,
 *   query SQL ou tela foi implementada nesta etapa — apenas a
 *   estrutura, a responsabilidade e a posição do arquivo dentro da
 *   arquitetura estão definidas aqui. A implementação virá em uma
 *   etapa de desenvolvimento futura.
 */

return [
    // 'nome_sistema' => 'Pitter Pan',
    // 'url_base'     => 'http://localhost/pitterpan-sistema/public',
    // 'ambiente'     => 'desenvolvimento',
    // 'timezone'     => 'America/Sao_Paulo',
    // Valores reais serão definidos na etapa de desenvolvimento.
];
